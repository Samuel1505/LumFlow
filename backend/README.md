# LumFlow Backend (scaffold)

This is a minimal Express + TypeScript scaffold for the LumFlow backend API. It provides a small `/api/streams` router used by the frontend and SDK during development.

Quick start:

```bash
cd backend
npm install
npm run dev
```
