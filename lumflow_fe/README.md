# LumFlow Frontend (scaffold)

Minimal Next.js (App Router) TypeScript scaffold for the LumFlow frontend. Connects to the local backend at `http://localhost:3001` by default.

Quick start:

```bash
cd lumflow_fe
npm install
npm run dev
```
